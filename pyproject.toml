[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scientific-computing-system-2.0"
version = "5.2.4"
description = "A NumPy/SciPy/Pandas/Matplotlib-powered scientific computing platform: accelerated linear algebra, statistics, optimization, integration, interpolation, signal processing, Monte Carlo, graphs (with PageRank), machine learning, time series, visualization and I/O."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.10"
authors = [
    {name = "Furox-Art", email = "furkanarkn1451@gmail.com"},
]
keywords = [
    "scientific-computing",
    "numpy",
    "scipy",
    "pandas",
    "matplotlib",
    "machine-learning",
    "signal-processing",
    "statistics",
    "optimization",
    "monte-carlo",
    "graph-theory",
    "pagerank",
    "time-series",
    "numerical-methods",
    "data-analysis",
]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy>=1.26",
    "scipy>=1.11",
    "pandas>=2.2",
    "matplotlib>=3.8",
]

[project.optional-dependencies]
dev = [
    "openpyxl",
    "pyarrow",
    "pandas-stubs>=2.0",
    # Cap numpy under 2.5 for the dev toolchain only: numpy>=2.5 stubs use
    # `type` statements that mypy cannot parse with python_version = "3.10".
    # The runtime dependency floor (numpy>=1.26) is unchanged.
    "numpy>=1.26,<2.5",
    "pytest>=8.0",
    "pytest-cov",
    "ruff>=0.4",
    "mypy>=1.10",
    # Independent oracle for tests/test_graph_oracles.py. Dev-only: cds2 never
    # imports networkx at runtime, it is used purely to check pagerank and
    # betweenness against a second implementation on weighted graphs.
    "networkx>=3.0",
    # Property-based testing (hypothesis) and sklearn estimator interface.
    "hypothesis>=6.0",
    "scikit-learn>=1.4",
]
test = [
    "pytest>=8.0",
    "pytest-cov",
    "hypothesis>=6.0",
    "scikit-learn>=1.4",
]
gpu = [
    "cupy-cuda12x",
]
docs = [
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.26",
]
all = [
    "openpyxl",
    "pyarrow",
    "pandas-stubs>=2.0",
    "numpy>=1.26,<2.5",
    "pytest>=8.0",
    "pytest-cov",
    "ruff>=0.4",
    "mypy>=1.10",
    "networkx>=3.0",
    "mkdocs>=1.6",
    "mkdocs-material>=9.5",
    "mkdocstrings[python]>=0.26",
    "hypothesis>=6.0",
    "scikit-learn>=1.4",
    "cupy-cuda12x",
]

[project.scripts]
cds2 = "cds2.cli:main"

[project.urls]
Homepage = "https://github.com/Furox-Art/scientific-computing-system-2.0"
Repository = "https://github.com/Furox-Art/scientific-computing-system-2.0"
Issues = "https://github.com/Furox-Art/scientific-computing-system-2.0/issues"
Changelog = "https://github.com/Furox-Art/scientific-computing-system-2.0/releases"

[tool.setuptools.packages.find]
where = ["src"]
include = ["cds2*"]

[tool.setuptools.package-data]
cds2 = ["py.typed"]

[tool.mypy]
python_version = "3.10"
strict = true
files = ["src/cds2"]

[[tool.mypy.overrides]]
# SciPy and Matplotlib ship no inline types or stubs; their APIs are used
# through thin wrappers so the Any leakage is contained at call boundaries.
module = ["scipy.*", "matplotlib.*", "openpyxl.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
# Optional soft dependencies: imported lazily inside functions so the
# package works without them. No stubs exist for these modules.
module = ["cupy", "cupy.*", "array_api_compat", "array_api_compat.*"]
ignore_missing_imports = true

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
pythonpath = ["src", "."]
addopts = ["--strict-markers", "--strict-config"]
markers = [
    "regression: benchmark regression gate (requires --regression)",
    "property: property-based test (hypothesis)",
]

[tool.coverage.run]
source = ["src/cds2"]
branch = true
# GPU submodules need CuPy + CUDA hardware, unavailable on CI runners.
# Their CPU mirrors in cds2.linalg / cds2.signals / cds2.montecarlo carry
# the numerical tests instead.
omit = ["src/cds2/gpu/linalg.py", "src/cds2/gpu/montecarlo.py", "src/cds2/gpu/signal.py"]

[tool.coverage.report]
show_missing = true
fail_under = 100
exclude_lines = [
    "pragma: no cover",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
